Here I test GitHub

Commands so far:
git init . 			// Initialises the repository
git add -A			// Adds all current files on the folder to the repo
git commit -a -m "message" 	// Commits all the changes and removes any deleted files, with commit message
git checkout -b <new>		// Creates a branch called new and transfers the work there
git branch			// Shows all the branches and the working one
git checkout <branch>		// Switches to another branch
git status			//Shows the status			